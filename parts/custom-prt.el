;;; custom-prt.el --- Customizations setup

;;; Commentary:
;;

;;; Code:

(setq custom-file (expand-file-name "customize.el" user-emacs-directory))
(load custom-file)

(provide 'custom-prt)

;;; custom-prt.el ends here
